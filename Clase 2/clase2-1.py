class LLave:
    def __init__(self, tamanio, color, forma):
        print(f"La llave ha sido creada! con el tamanio: {tamanio} posee el color: {color} por ultimo posee la siguiente forma: {forma}")

jl = LLave(1.56, "azul", "cuadrada")